# Free Gemini API key — https://aistudio.google.com/apikey  (no credit card needed)
GEMINI_API_KEY=

# Optional overrides:
# GEMINI_MODEL=gemini-3.5-flash   # confirm the current free Flash name in AI Studio
# SPIKE_PACING_MS=5000            # delay between calls to stay under ~15 RPM free tier
