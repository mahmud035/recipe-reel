# Client configuration. The dev server proxies /api to the Express server
# (see vite.config.ts), so LEAVE THIS UNSET for local development.
#
# Production only (Vercel split-origin): set to the API server ORIGIN — no trailing /api.
# VITE_API_BASE_URL=https://lab-recipe-reel.halalaura.co.uk
