{
  "name": "recipe-reel-spike",
  "private": true,
  "type": "module",
  "description": "Phase 0 spike: measure quantity-capture rate of Gemini Flash on real Bengali cooking videos (text-only vs multimodal). Throwaway harness, not the app.",
  "scripts": {
    "spike": "node --env-file=.env run.ts"
  },
  "dependencies": {
    "@google/genai": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0"
  }
}
